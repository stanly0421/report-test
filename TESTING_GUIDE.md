# 測試指南 - 字幕滾動跟隨功能

## 修復內容

修復了字幕顯示區域在更新時總是回到最上面的問題。現在字幕會自動滾動以保持當前播放的字幕可見。

## 測試步驟

### 前置條件

1. 確保已安裝 Qt 5.x 或 Qt 6.x 開發環境
2. 建置專案：
   ```bash
   mkdir build
   cd build
   cmake ..
   cmake --build .
   ```
3. 準備測試檔案：
   - 一個音樂檔案（如 .mp3, .wav 等）
   - 對應的 .srt 字幕檔案（建議至少有 10 個以上的字幕條目）

### 測試案例 1：自動播放時的字幕跟隨

**目的**：驗證音樂播放時，字幕顯示區域會自動滾動跟隨當前字幕

**步驟**：
1. 啟動應用程式
2. 點擊「載入音樂檔案」並選擇測試音樂
3. 點擊「載入字幕檔案」並選擇對應的 .srt 字幕
4. 點擊播放按鈕開始播放
5. 觀察字幕顯示區域

**預期結果**：
- ✅ 當前播放的字幕應該以綠色背景高亮顯示
- ✅ 字幕顯示區域應該自動滾動，保持高亮的字幕在可見範圍內
- ✅ 隨著音樂播放，高亮的字幕應該持續保持在可見範圍內，不會消失到視窗上方或下方

**失敗表現（修復前）**：
- ❌ 每次字幕更新時，顯示區域都會跳回到最上面
- ❌ 無法持續看到當前播放的字幕

### 測試案例 2：點擊字幕跳轉後的跟隨

**目的**：驗證點擊字幕時間戳跳轉後，字幕顯示區域會保持在跳轉位置

**步驟**：
1. 在播放音樂時，手動向下滾動字幕顯示區域
2. 點擊一個位於下方的字幕時間戳（例如 [30.5s - 35.2s]）
3. 觀察播放位置和字幕顯示區域

**預期結果**：
- ✅ 音樂應該跳轉到對應的時間點
- ✅ 視窗標題應該短暫顯示「跳轉到 XX:XX」的提示（2 秒後恢復）
- ✅ 字幕顯示區域應該停留在點擊的字幕附近，能看到該字幕被高亮
- ✅ 隨著音樂繼續播放，字幕應該持續自動滾動跟隨

**失敗表現（修復前）**：
- ❌ 跳轉後字幕顯示區域會回到最上面
- ❌ 看不到剛才點擊跳轉到的字幕

### 測試案例 3：手動滾動的保留

**目的**：驗證用戶手動滾動查看其他字幕時的行為

**步驟**：
1. 在播放音樂時，使用滾動條或滑鼠滾輪向下滾動查看後面的字幕
2. 停留約 5 秒（讓字幕同步計時器觸發幾次）
3. 觀察字幕顯示區域的行為

**預期結果**：
- ✅ 當字幕索引更新時，顯示區域會自動滾動回到當前播放的字幕
- ✅ 這是預期行為，因為自動跟隨功能應該優先於手動滾動

**注意**：
- 如果用戶想查看其他字幕，可以暫停播放，這樣字幕同步計時器會停止，不會自動滾動

### 測試案例 4：暫停和恢復播放

**目的**：驗證暫停和恢復播放時的字幕行為

**步驟**：
1. 播放音樂並讓字幕滾動到中間位置
2. 點擊暫停按鈕
3. 手動滾動字幕到其他位置
4. 點擊播放按鈕恢復播放
5. 觀察字幕顯示區域

**預期結果**：
- ✅ 暫停時，字幕停止更新，保持在暫停時的高亮狀態
- ✅ 暫停時，用戶可以自由滾動查看其他字幕，不會被打斷
- ✅ 恢復播放後，字幕會在下一次更新時（最多 100ms 後）滾動回到當前播放位置

### 測試案例 5：切換歌曲

**目的**：驗證切換到其他歌曲時的清理行為

**步驟**：
1. 播放一首有字幕的歌曲
2. 雙擊播放清單中的另一首歌曲切換
3. 觀察字幕顯示區域

**預期結果**：
- ✅ 舊歌曲的字幕應該被清除
- ✅ 如果新歌曲有字幕，應該載入新字幕並從頭開始同步
- ✅ 字幕顯示區域應該回到頂部（新歌曲開始）

## 效能測試

### 測試長字幕檔案

**目的**：驗證對長字幕檔案的處理效能

**步驟**：
1. 準備一個有 100+ 條字幕的 .srt 檔案
2. 載入並播放音樂
3. 觀察 CPU 使用率和 UI 響應速度

**預期結果**：
- ✅ CPU 使用率應該保持在合理範圍（通常 < 5%）
- ✅ 字幕更新應該流暢，沒有明顯延遲或卡頓
- ✅ UI 操作（點擊按鈕、拖動進度條等）應該保持響應

## 已知限制

1. **自動滾動優先**：當音樂播放時，字幕會自動滾動到當前位置。如果用戶想查看其他字幕，建議先暫停播放。

2. **Qt 版本相容性**：此功能在 Qt 5.x 和 Qt 6.x 中都應該正常工作，因為 `scrollToAnchor()` 是 QTextBrowser 的標準功能。

## 報告問題

如果發現任何問題，請提供以下資訊：
- Qt 版本
- 作業系統
- 問題的詳細描述
- 如何重現問題的步驟
- 字幕檔案的大小（條目數量）
